{
  "testname": "GET_ping_204",
  "description": "GET /v1/ping test by ${env}.USER starting at ${timestamp}",
  "expectation": "Return 204 if server is running",
  "status": "204",
  "responseRegexp": ".*",
  "scheme": "http",
  "options": {
    "host": "localhost",
    "port": "3000",
    "path": "/v1/ping",
    "method": "GET"
  }
}
