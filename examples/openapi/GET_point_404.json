{
  "testname": "GET_point_404",
  "description": "GET /v1/point/{point_id}",
  "expectation": "should return 404 not found for unknown point",
  "status": "404",
  "responseRegexp": ".*",
  "scheme": "http",
  "options": {
    "host": "localhost",
    "port": "3000",
    "path": "/v1/point/{point_id}",
    "method": "GET"
  }
}